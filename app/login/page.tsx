export default function LoginPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-white text-black">
      <div className="text-center">
        <h1 className="text-2xl font-bold">Empty Test Page</h1>
        <p className="mt-2">If you can see this, the crash was inside your old Login Page code.</p>
        <p className="mt-2 text-sm text-gray-500">If you still see an error, the problem is in app/layout.tsx.</p>
      </div>
    </div>
  )
}
